# Documentation

Phase specifications live here.

## Known gap

`CLAUDE.md` names `docs/phase-01-foundation.md` as the current-phase specification. That
file does not exist yet. It is recorded here as an open item rather than invented — the
authoritative story text currently lives in the Notion **User Stories** database, and the
per-story plans are under `.squad/plans/`.
