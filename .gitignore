# Managed by squad-kit — do not edit this block
.squad/secrets.yaml
.squad/stories/**/attachments/
.squad/.trash/
.squad/.last-run.json
.squad/.last-copy-prompt.md
.squad/runs/
# End squad-kit block

# Node
node_modules/
dist/
build/
.vite/
.tsbuild/
coverage/
*.tsbuildinfo

# Prisma client — generated by `postinstall`, never committed
backend/src/generated/

# Environment — never commit secrets
.env
.env.*
!.env.example
# Committed deliberately: localhost-only, throwaway credentials for the test
# database, so the suite runs from a clean clone. NEVER put a real credential
# in backend/.env.test.
!backend/.env.test

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
