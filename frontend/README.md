# `@crm/frontend`

Placeholder Vite + React scaffold. `src/App.tsx` exists only to prove the shared package
resolves and type-checks on the frontend side.

UI work begins in **Phase P03** — design tokens, Tailwind, shadcn/ui, React Router,
TanStack Query, and i18next all arrive there. None of them are installed yet.
